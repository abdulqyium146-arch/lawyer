import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        ink: "#0D1B2A",
        gold: "#B8973A",
        "gold-light": "#E8D5A3",
        "gold-rule": "#C9A84C",
        cream: "#F7F3EC",
        slate: "#3D5166",
        mist: "#EAE8E4",
        success: "#2D6A4F",
      },
      fontFamily: {
        cormorant: ["var(--font-cormorant)", "Georgia", "serif"],
        outfit: ["var(--font-outfit)", "system-ui", "sans-serif"],
      },
      fontSize: {
        h1: "clamp(2.2rem, 5vw, 4.2rem)",
        h2: "clamp(1.6rem, 3.5vw, 2.8rem)",
        h3: "1.4rem",
      },
      boxShadow: {
        card: "0 1px 3px rgba(13,27,42,0.08)",
        "card-hover":
          "0 4px 16px rgba(13,27,42,0.12), -3px 0 0 0 #B8973A",
        nav: "0 1px 0 #EAE8E4, 0 2px 8px rgba(13,27,42,0.06)",
      },
      keyframes: {
        fadeUp: {
          "0%": { opacity: "0", transform: "translateY(20px)" },
          "100%": { opacity: "1", transform: "translateY(0)" },
        },
        goldPulse: {
          "0%, 100%": { opacity: "1" },
          "50%": { opacity: "0.7" },
        },
      },
      animation: {
        fadeUp: "fadeUp 0.5s ease forwards",
        goldPulse: "goldPulse 2s ease-in-out infinite",
      },
      backgroundImage: {
        grain:
          "url(\"data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E\")",
      },
      clipPath: {
        diagonal: "polygon(0 0, 100% 0, 100% 92%, 0 100%)",
        "diagonal-bottom": "polygon(0 8%, 100% 0, 100% 100%, 0 100%)",
      },
    },
  },
  plugins: [],
};

export default config;
